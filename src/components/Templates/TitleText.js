const TitleText = ({children}) => {
    return(
        <h1 style = {{fontFamily : 'Raleway', color : '#e86851', fontSize : '2rem', fontWeight : 'normal'}}>{children}</h1>
    )
}

export default TitleText;